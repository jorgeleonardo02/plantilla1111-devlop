/* import { Component, OnInit } from '@angular/core';
import { Router } from '@angular/router';
import { Contenido } from '../contenido/contenido';
import { ContenidoService } from '../contenido/contenido.service';
import { TokenService } from '../seguridad/service/token.service';

@Component({
  selector: 'app-menu',
  templateUrl: './menu.component.html',
  styleUrls: ['./menu.component.css']
})
export class MenuComponent implements OnInit { */

  /* listaContenidos: Contenido[];

  constructor(private contenidoService: ContenidoService,
              public tokenService: TokenService,
              private router: Router) {}
  ngOnInit(): void{}

  vengoDeContenido(){ */
    /* this.listaContenidos=this.contenidoService.entregarListaContenidos(); */
    /* console.log("lista contenidos");
    console.log(this.listaContenidos); */
  /*   this.contenidoService.recogerListaDeContenidos(this.listaContenidos);
    this.contenidoService.clickDesdeFuera.next(true);
  }
}
 */

