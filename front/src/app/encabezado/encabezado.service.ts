import { Injectable } from '@angular/core';
import { Subject } from 'rxjs';

/* @Injectable({
  providedIn: 'root'
})
export class EncabezadoService {
  
  private pathActual = new Subject<string>
  path$ = this.pathActual.asObservable();

  private idCategoria: number;
  
  constructor() { }

  public actualizarPath(path: string){
    this.pathActual.next(path);
  }
  public setIdCategoria(id: number): void{
    this.idCategoria = id;
  }
  public getIdCategoria(): number {
    return this.idCategoria;
  } 
} */
